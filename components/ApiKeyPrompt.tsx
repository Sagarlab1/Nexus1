// FIX: Implement the ApiKeyPrompt component to resolve module errors.
import React from 'react';

const ApiKeyPrompt: React.FC = () => {
  /**
   * This component is intentionally left blank to comply with the requirement
   * of not having any UI for API key management. Per the guidelines, the API 
   * key is sourced exclusively from `process.env.API_KEY`.
   */
  return null;
};

export default ApiKeyPrompt;
